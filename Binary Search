BinarySearch
============

1) the array are already shorted for binary search while doing this opration ..
2) you have multiple way for doing this opration ...




while(low<=high)
{
mid = floor(low+high)/2 ;

if( arr[mid] == key)
return mid ;

else if (arr[mid] > key ) 

high = mid - 1 ;


else 

low = mid +1 ; 

}

return -1 ;


You can call it recursivally ..
--------------------------------
function binarysearch(low , high) {
if(low>key ){

}
}
